class Quote {
  String text;
  String author;

  // Quote({String text, String author}) {
  //   this.text = text;
  //   this.author = author;
  // }

  Quote({required this.text, required this.author});
}

// Quote q = Quote('welcome quote', 'jhon');
// Quote q = Quote(text: 'welcome quote', author: 'jhon');
